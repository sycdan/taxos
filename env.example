# Copy this file to .env and edit the values as needed.

# Root of the project as seen by the Docker daemon.
# On Linux / Codespaces the default (.) works fine so you can leave this unset.
# On Windows (Docker running via WSL), set this to the Windows-side path, e.g.:
# CODE_ROOT=/c/Users/Dan/Projects/taxos
CODE_ROOT=
