# Today's Task

We need to consolidate the dashboard and buckets into a single view. 

We should have a single view that shows all buckets and their allocated totals in the selected date range. 

There should be a button to add a new bucket.

The user should be able to rename or delete a bucket after clicking on it to go to the bucket details view.

