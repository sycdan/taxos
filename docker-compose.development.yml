services:
  dev:
    build:
      context: .
      dockerfile: dev/Dockerfile
    volumes:
      - .:/workspaces/taxos
      - /var/run/docker.sock:/var/run/docker.sock
    working_dir: /workspaces/taxos
    user: "vscode"
